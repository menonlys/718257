# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]  
    # Folder in which original videos are stored
    videos-folder: # default is videos
    # New format for videos
    new-format: # default is mp4
    # Folder in which converted videos are stored
    output-folder: # default is output
    - name: Video Converter Action
  # You may pin to the exact commit or the version.
  # uses: umuthan/video-converter@ad753f854ba38ddabb382233c254579e31b3df75
  uses: umuthan/video-converter@v1.0
  with:
    # Folder in which original videos are stored
    videos-folder: # default is videos
    # New format for videos
    new-format: # default is mp4
    # Folder in which converted videos are stored
    output-folder: # default is output
    - name: Cypress Screenshot & Video Upload to WorkChat
  # You may pin to the exact commit or the version.
  # uses: SreeniIO/cypress-upload-to-workchat-action@892527fcd4931e66366d2ea5efa85754935e0308

  uses: hoangsaint/cypress-upload-to-workchat-action@v1.0.8
  with:
    # WorkChat token
    token: 
    # WorkChat channel
    channel: 
    # The path of the directory where Cypress stores its screenshots. Defaults to `./cypress`. No trailing slashes, please.
    workdir: # optional
    # Custom WorkChat message text
    message-text: # optional
    - name: Cypress Screenshot & Video Upload to Slack
  # You may pin to the exact commit or the version.
  # uses: hoangsaint/cypress-slack-video-upload-action@572c3c6a97f3e951bb0ba0318d1b235cd8927d28
  uses: hoangsaint/cypress-slack-video-upload-action@v1.3.0
  with:
    # Slack token
    token: 
    # Slack channels
    channels: 
    # The path of the directory where Cypress stores its screenshots. Defaults to `./cypress`. No trailing slashes, please.
    workdir: # optional
    # Custom Slack message text
    message-text: # optional
    
    - name: Research Project Action
  # You may pin to the exact commit or the version.
  # uses: Jonas-D-M/Webproject-recorder@b53bc7f1fbadb67fe045fa1824b290e67f6cd3f2
  uses: Jonas-D-M/Webproject-recorder@v1.1.0
  with:
    # GitHub token
    token: # optional
    # Path to the project
    project-dir: # optional
    
    - name: Video Converter Action
  # You may pin to the exact commit or the version.
  # uses: umuthan/video-converter@ad753f854ba38ddabb382233c254579e31b3df75
  uses: umuthan/video-converter@v1.0
  with: hoangsaint/video-converter@v1.0
    # Folder in which original videos are stored
    videos-folder: # default is videos
    # New format for videos
    new-format: # default is mp4
    # Folder in which converted videos are stored
    output-folder: # default is output
    
    - name: VHS Action
  # You may pin to the exact commit or the version.
  # uses: charmbracelet/vhs-action@e241d0e6a4ada35f2c3b8d10fcd6da4c865b675f
  uses: charmbracelet/vhs-action@v2.0.0
  with:
   with: hoangsaint/vhs-action@v2.0.0
    # VHS version
    version: # optional, default is latest
    # The tape file path
    path: # optional, default is 
    # Used to pull VHS from Github
    token: # optional, default is ${{ github.token }}
    # Install extra fonts
    install-fonts: # optional, default is false
    - name: Upload a Build Artifact
  uses: actions/upload-artifact@v3.1.3
  with:  hoangsaint/upload-artifact@v3.1.3
    # Artifact name
    name: # optional, default is artifact
    # A file, directory or wildcard pattern that describes what to upload
    path: 
    # The desired behavior if no files are found using the provided path.
Available Options:
  warn: Output a warning but do not fail the action
  error: Fail the action with an error message
  ignore: Do not output any warnings or errors, the action does not fail

    if-no-files-found: # optional, default is warn
    # Duration after which artifact will expire in days. 0 means using default retention.
Minimum 1 day. Maximum 90 days unless changed from the repository settings page.

    retention-days: # optional
    - name: Setup Java JDK
                uses: actions/setup-java@v3.13.0
            
    - name: Setup Java JDK
  uses: actions/setup-java@v3.13.0
  with:
    # The Java version to set up. Takes a whole or semver Java version. See examples of supported syntax in README file
    java-version: # optional
    # The path to the `.java-version` file. See examples of supported syntax in README file
    java-version-file: # optional
    # Java distribution. See the list of supported distributions in README file
    distribution: 
    # The package type (jdk, jre, jdk+fx, jre+fx)
    java-package: # optional, default is jdk
    # The architecture of the package (defaults to the action runner's architecture)
    architecture: # optional
    # Path to where the compressed JDK is located
    jdkFile: # optional
    # Set this option if you want the action to check for the latest available version that satisfies the version spec
    check-latest: # optional
    # ID of the distributionManagement repository in the pom.xml file. Default is `github`
    server-id: # optional, default is github
    # Environment variable name for the username for authentication to the Apache Maven repository. Default is $GITHUB_ACTOR
    server-username: # optional, default is GITHUB_ACTOR
    # Environment variable name for password or token for authentication to the Apache Maven repository. Default is $GITHUB_TOKEN
    server-password: # optional, default is GITHUB_TOKEN
    # Path to where the settings.xml file will be written. Default is ~/.m2.
    settings-path: # optional
    # Overwrite the settings.xml file if it exists. Default is "true".
    overwrite-settings: # optional, default is true
    # GPG private key to import. Default is empty string.
    gpg-private-key: # optional
    # Environment variable name for the GPG private key passphrase. Default is $GPG_PASSPHRASE.
    gpg-passphrase: # optional
    # Name of the build platform to cache dependencies. It can be "maven", "gradle" or "sbt".
    cache: # optional
    # Workaround to pass job status to post job step. This variable is not intended for manual setting
    job-status: # optional, default is ${{ job.status }}
    # The token used to authenticate when fetching version manifests hosted on github.com, such as for the Microsoft Build of OpenJDK. When running this action on github.com, the default value is sufficient. When running on GHES, you can pass a personal access token for github.com if you are experiencing rate limiting.
    token: # optional, default is ${{ github.server_url == 'https://github.com' && github.token || '' }}
    # Name of Maven Toolchain ID if the default name of "${distribution}_${java-version}" is not wanted. See examples of supported syntax in Advanced Usage file
    mvn-toolchain-id: # optional
    # Name of Maven Toolchain Vendor if the default name of "${distribution}" is not wanted. See examples of supported syntax in Advanced Usage file
    mvn-toolchain-vendor: # optional
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
